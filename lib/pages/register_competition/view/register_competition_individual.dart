import 'package:flutter/material.dart';

class RegisterCompetitionIndividual extends StatelessWidget {
  const RegisterCompetitionIndividual({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
